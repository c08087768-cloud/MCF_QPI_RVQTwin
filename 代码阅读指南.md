# 代码阅读指南

本文件只解决一个问题：**拿到这份代码后，按什么顺序读、读哪些文件、怎么把调用链串起来。**

项目背景、研究路线、数据集说明、损失公式等已在 `START_HERE_中文.md` 和 `docs/01–13_*.md` 里讲清楚，本指南不重复，只补"读代码的地图"。所有路径相对 `MCF_QPI_RVQTwin/`。

---

## 0. 先读这 4 个文件（30 分钟，建立全局观）

按顺序读，不要跳：

1. `START_HERE_中文.md` — 任务定义、论文主线、一次完整执行的命令。**先确认任务是什么**（散斑强度图 → 二维相位图，标签来自 MNIST/Fashion 映射到 `[0,π]`）。
2. `docs/12_代码框架与文件职责.md` — 每个目录/文件一句话职责，是本指南的"目录页"。
3. `docs/04_网络设计与损失函数.md` — 网络结构和总损失公式。**读完再去看 `models/`，否则看不懂为什么这么搭。**
4. `docs/13_完整训练评估决策树.md` — 训练顺序 A→I、8 种测试协议。决定了你"应该按什么顺序读训练/评估脚本"。

读完这 4 个，你应该能回答：任务输入输出是什么、主模型由哪几部分拼起来、训练分几步、评估有几套协议。

---

## 1. 整体架构一张图

```text
configs/*.yaml          ← 所有实验参数（先看这一个 yaml 就懂了全流程）
        │  load_config()
        ▼
scripts/train_*.py      ← 命令行入口，串起下面三块
        │
        ├─ data/    build_dataset() → HDF5 配对数据
        ├─ factory  build_inverse_model() → 按 model.type 选网络
        │             build_forward_twin() / build_diffusion()
        └─ training/engine.fit_model() ← 通用训练循环（AMP/早停/ckpt/TensorBoard）
                │  step_function 由各 train_*.py 注入
                ▼
scripts/evaluate_*.py   ← 加载 best.pt，跑 evaluation/runner
        ▼
outputs/<exp>/          ← best.pt, history.json, per_sample_metrics.csv, png
```

**关键设计：** `engine.fit_model` 是通用的，不知道在训什么网络；具体前向/损失通过 `step_function` 闭包注入（见 `scripts/train_inverse.py:47` 的 `step`）。所以读任何训练脚本，重点是看它怎么定义 `step` 和构造了哪些模型。

---

## 2. 推荐阅读顺序（按"数据流"走，不是按字母序）

### 第 1 站：配置 —— 先读 yaml 再读代码

- `configs/research/proposed_rvqtwin.yaml`（主模型配置，最全）
- `configs/smoke/proposed.yaml`（CPU 小配置，先跑这个）

yaml 的顶层键就是代码的顶层 config 键：`data / loader / model / forward_twin / loss / training / evaluation`。**读 yaml 时对照 `scripts/train_inverse.py` 看每个键被谁消费。**

配置加载逻辑很简单：`src/mcfqpi/config.py` 的 `load_config()` = 读 yaml + `apply_overrides()` 解析 `--set key.subkey=value`。命令行覆盖是点分路径，改实验不用动 yaml。

### 第 2 站：数据管线（`src/mcfqpi/data/`）

这是最容易出错、也最值得先读的部分（记忆里记过这里的 pairing/audit 修过 bug）。按数据流顺序：

```text
download.py   官方 Figshare 元数据 + 断点续传
   ↓
archive.py    防 path-traversal 的 tar 解压
   ↓
pairing.py    ★ 扫文件 → 识别 speckle/phase → 按共同索引配对
   ↓
split.py      官方 split 继承 / 哈希分组严格划分
   ↓
audit.py      重复、缺失、跨集合泄漏检查
   ↓
transforms.py  speckle 归一化、相位 [0,π]↔[0,1] 解码、fit-pad、传感器扰动
   ↓
cache.py      预处理 → HDF5（data/processed/mcf_qpi_128.h5）
   ↓
dataset.py    ★ HDF5Dataset：CSV 索引 + worker 延迟打开 h5 句柄
```

★ 两个重点：`pairing.py`（怎么从混乱的真实文件名推出配对）和 `dataset.py`（`build_dataset()` 是所有训练脚本的入口，看它返回什么 batch 字段：`speckle` / `phase` / 域标签等）。

配套脚本：`scripts/run_data_preparation.sh` 串起整条管线；`scripts/inspect_raw_dataset.py` / `visualize_pairs.py` 用来人工抽查配对（**START_HERE 反复强调必须人工抽查**）。

### 第 3 站：模型（`src/mcfqpi/models/`）

按"从简单到复杂、从独立到组合"的顺序读：

1. `blocks.py` — 共享积木：残差块、下/上采样、`set_requires_grad`。先扫一遍，后面会反复用到。
2. `resunet.py` — **基线，先读懂这个**。`ResUNetPhase` 输出 `phase` + `log_scale`（Laplace 不确定度）。读懂基线才知道主模型在改什么。
3. `rvq.py` — Residual Vector Quantization 核心。重点看：多级码本怎么逐级量化残差、commitment loss、perplexity/死码怎么算。公式见 `docs/04 §2`。
4. `phase_rvqvae.py` — 只在相位标签上预训练的 `phase → encoder → RVQ → decoder → phase`。**这是主模型的"先验"**，单独训、单独存 `best.pt`。
5. `forward_twin.py` — 经验 `phase → speckle` 前向代理。单独训，主模型可选挂载做 cycle loss。注意它**不是**物理传播模型。
6. `dual_domain_rvq_twin.py` — **主模型，最后读**。把上面几样拼起来：空间域 + FFT 对数幅度双编码 → 门控融合 → 投影到相位码本 + 连续残差 → 解码 + 不确定度。读这个文件时回头对照 `docs/04 §3-4` 的公式。
7. `diffusion.py` — 条件 DDPM/DDIM 基线，独立支线，最后看。

`src/mcfqpi/factory.py` 是模型的"开关"：`build_inverse_model()` 按 `config["model"]["type"]` 选 resunet / dual_domain_rvq_twin，主模型还会 `load_phase_prior()` 加载预训练码本。**读 factory 能快速看清模型间的组装关系。**

### 第 4 站：训练（`src/mcfqpi/training/`）

- `losses.py` — 先读 `LossWeights.from_dict()`（对应 yaml 的 `loss:` 段）和 `InverseLoss`。总损失各项见 `docs/04 §6`，代码里每一项权重都能在 yaml 里找到。
- `engine.py` — 通用训练循环。重点函数：
  - `build_loader()` — DataLoader 构造（worker 种子、pin_memory、drop_last）
  - `fit_model()` — 主循环：AMP、梯度累积/裁剪、step/epoch scheduler、早停、checkpoint、TensorBoard
  - 留意 `step_function` 参数 —— 真正的前向+损失由调用方注入，engine 本身网络无关。

### 第 5 站：评估（`src/mcfqpi/evaluation/`）

- `metrics.py` — 单样本指标：normalized/radian MAE、RMSE、SSIM、PSNR、相关系数、梯度误差。
- `runner.py` — 逐样本写 CSV、bootstrap 置信区间、分域统计、MC dropout 不确定度、risk–coverage 曲线。
- `robustness.py` — 可复现的传感器扰动（对应 `docs/13 Test-6`）。
- `visualization.py` — 定性图 `qualitative_examples.png`。

### 第 6 站：脚本入口（`scripts/`）

训练/评估脚本是粘合层，逻辑都在包里。读法：**每个脚本只看 `main()` 的骨架**，看它调了哪些 `build_*` / `fit_model` / `evaluate`，不用细读。

| 脚本 | 作用 | 对应 train 顺序 |
|------|------|----------------|
| `train_phase_rvqvae.py` | 训相位先验 | B |
| `evaluate_phase_prior.py` | 评估先验重建上限/码本占用 | C |
| `train_forward_twin.py` | 训经验前向代理 | D |
| `evaluate_forward_twin.py` | 独立验证前向代理 | E |
| `train_inverse.py` | 训 ResUNet 基线 / 主模型（看 `model.type`） | F/G/H |
| `train_diffusion.py` | 训扩散基线 | I |
| `evaluate.py` | 通用评估入口 | — |
| `evaluate_cross_domain.py` / `evaluate_robustness.py` / `evaluate_mean_phase.py` | 各测试协议 | Test-2/6/sanity |

一键流程脚本（`.sh`）把上面串起来：`run_smoke_test.sh` → `run_data_preparation.sh` → `run_research_pipeline.sh` → `run_ablations.sh` → `run_diffusion_baseline.sh` → `run_three_seeds.sh`。

---

## 3. 三个"读懂就通了"的调用链

如果时间紧，只追这三条链，追完整个项目就通了。

### 链 A：一次训练从头到尾
```
scripts/train_inverse.py main()
  → load_config(yaml, --set)              # config.py
  → build_dataset(data, "train"/"val")    # data/dataset.py
  → build_inverse_model(model cfg)        # factory.py → resunet.py 或 dual_domain_rvq_twin.py
  → InverseLoss(LossWeights.from_dict())  # training/losses.py
  → [可选] build_forward_twin + load weights + freeze
  → fit_model(model, loaders, step, ...)  # training/engine.py
```
重点看 `scripts/train_inverse.py:47` 的 `step` 闭包：`model(speckle, target_phase=phase)` → `criterion(outputs, batch, forward_twin=...)`。这一行就是"前向+损失"的全部。

### 链 B：主模型前向
```
DualDomainRVQTwin.forward(speckle, target_phase)
  ├─ 空间域分支 → z_s
  ├─ FFT → log(1+|F|) → 归一化 → 频域分支 → z_f
  ├─ 门控融合: z = g⊙z_s + (1-g)⊙z_f + R([z_s,z_f])
  ├─ 投影到冻结的相位 RVQ 码本: Q_RVQ(z_speckle)
  ├─ 连续残差: + α·tanh(R(z_speckle))
  ├─ 相位解码器 → phase
  └─ 不确定度头 → log_scale
```
对照 `docs/04 §3-4`。注意 `freeze_prior_encoder / freeze_codebook` 在 `factory.build_inverse_model` 里设——先验码本默认冻结，只训融合+残差+解码器。

### 链 C：一次评估
```
scripts/evaluate.py
  → load_model_weights(model, best.pt)    # factory.py
  → evaluation/runner.py: 逐样本 metrics → CSV
  → bootstrap CI、分域统计、MC dropout、risk-coverage
  → summary.json + qualitative_examples.png
```

---

## 4. 读代码时的几个抓手

- **yaml 是路标。** 任何脚本看不懂在配什么，回去看对应 yaml 的同名列。
- **`outputs/<exp>/resolved_config.yaml` 是真相。** 训练后会把"yaml + 命令行覆盖"合并存一份，复现实验以它为准，不是原始 yaml。
- **`docs/09_故障排查.md`** 放手边，遇到报错先查。
- **测试也是文档。** `tests/` 下每个文件对应一个模块的预期行为（pairing/split/transforms/losses/metrics/models/hdf5），读代码时拿测试当"用法示例"。
- **记忆里记过的 3 个已修 bug**（pairing 推理、audit tuple 键、ablation `device: auto→cuda`）—— 读 `pairing.py`、`audit.py`、`configs/ablations/*.yaml` 时这些地方是踩过坑的，值得多看一眼。

---

## 5. 建议的第一次实操（边读边跑）

1. `conda activate mcf-qpi`（环境已建好，见记忆）
2. `bash scripts/run_smoke_test.sh` —— 跑通最小链路，看 `outputs/smoke/` 产物
3. 边跑边对照本指南第 2 节读 `smoke` 配置和对应脚本
4. 跑通后数据已在 `data/processed/mcf_qpi_128.h5`，再按第 2 节顺序深读 `data/` → `models/` → `training/` → `evaluation/`

读完这套代码，你应该能：改一个 yaml 跑出新实验、写一个新的 `evaluate_*.py` 加一种测试协议、定位某个 loss 项是哪行算的。
