# MCF-QPI 服务器部署指南（GPU）

本文档记录把本项目从本地（CPU，已验证）迁移到 GPU 服务器上正式训练的完整步骤。
所有命令在 **Linux 服务器** 上执行，使用 conda。

---

## 0. 当前状态（本地已完成的验证）

- 代码已在本机 CPU 上跑通：smoke test **17/17** 通过；真实数据 **50,176 对**已处理成
  `data/processed/mcf_qpi_128.h5`（1.9 GB）和 `data/processed/mcf_qpi_manifest.csv`（27 MB）。
- 已修复三处问题，**均已写入代码，无需在服务器上重做**：
  1. `src/mcfqpi/data/pairing.py`：配对关键词/驼峰/划分推理，适配真实 MCF-QPI 目录结构；
  2. `src/mcfqpi/data/audit.py`：`groupby(...).size().to_dict()` 产生 tuple 键导致 JSON 崩溃；
  3. 训练配置 `device: auto → cuda`，并在 `select_device()` 增加多卡选卡（`MCFQPI_DEVICE`）与索引越界保护（见第 8 节）。

---

## 1. 上传到服务器

### 方案 A：带上已处理好的数据（推荐，省时）
上传整个 `MCF-QPI_项目/MCF_QPI_RVQTwin/` 目录即可，重点包含：
```
data/processed/mcf_qpi_128.h5      # 1.9 GB，训练直接读它
data/processed/mcf_qpi_manifest.csv # 27 MB
src/ scripts/ configs/ tests/ ...
```

### 方案 B：只传代码 + 数据集，服务器上重新做数据准备
上传代码 + `MCF-QPI_数据集.zip`（740 MB），到服务器后跑第 5 节的数据准备。

> 建议用 `rsync` / `scp`，别用会改换行或编码的工具直接编辑 .py/.sh。

---

## 2. 服务器上确认 GPU

```bash
nvidia-smi
```

当前服务器配置：**2× NVIDIA RTX 3090 Ti，每张 24 GB 显存**，驱动 570.124.06，右上角 **CUDA Version: 12.8**。
看两点：
- 右上角 **CUDA Version**（驱动支持的最高 CUDA 版本，决定第 3 节装哪个 torch）。本机 12.8 → 装 cu124 或 cu126 都行（见第 3 节表）。
- 显存大小（决定 batch size 能开多大）。3090 Ti 24 GB 够大，主模型 `batch_size: 32` 直接可跑。

**选卡：默认用 0 号卡。** 要指定某张卡，**不要改 yaml**，在命令前加环境变量即可：

```bash
MCFQPI_DEVICE=cuda:0 bash scripts/run_smoke_test.sh      # 用 0 号卡
MCFQPI_DEVICE=cuda:1 SEED=42 bash scripts/run_research_pipeline.sh  # 用 1 号卡
```

机制见第 8 节。两张卡都空闲时，可在两个终端分别设 `cuda:0` / `cuda:1` 同时跑两个实验。

---

## 3. 建 conda 环境 + 装 CUDA 版 PyTorch

```bash
conda create -n mcf-qpi python=3.12 -y
conda activate mcf-qpi

# 按 nvidia-smi 的 CUDA Version 选 index（见下表）。本机 CUDA 12.8 → 推荐 cu126
pip install torch --index-url https://download.pytorch.org/whl/cu126

pip install -r requirements.txt
pip install -e .
```

| nvidia-smi CUDA Version | 推荐 torch index | 备注 |
|---|---|---|
| 11.8 | `https://download.pytorch.org/whl/cu118` | 驱动较老时 |
| 12.1 | `https://download.pytorch.org/whl/cu121` | |
| 12.4 | `https://download.pytorch.org/whl/cu124` | |
| **12.6 / 12.8（本机）** | `https://download.pytorch.org/whl/cu126` | **当前服务器用这个**；cu124 也能跑，但 cu126 与 12.8 驱动更匹配 |

> 代码不依赖 torchvision，只装 `torch` 即可。
> 项目自带的 `scripts/setup_env.sh` 是 venv 方案且默认 cu124；如果你习惯 venv，可
> `TORCH_INDEX_URL=https://download.pytorch.org/whl/cu126 bash scripts/setup_env.sh` 替代本节的 conda 方案。

---

## 4. 验证 GPU 可用（关键，不能跳过）

```bash
python -c "import torch; print('cuda:', torch.cuda.is_available(), '| torch.cuda:', torch.version.cuda, '| gpu:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'NONE')"
```

必须输出 `cuda: True`。如果 `False`，说明装成了 CPU 版或驱动不匹配——直接去第 9 节排查，
**不要继续**，否则正式训练会报错退出（这是预期的安全网，不是 bug）。

---

## 5. 数据准备

### 方案 A（已上传 HDF5）：跳过本节，直接进第 6 节。

### 方案 B（重新做数据准备）：
```bash
bash scripts/run_data_preparation.sh
```
它会按顺序：优先复用本地数据集 zip / 已下载的 tar → 解压 → 配对（已修好）→ 哈希 → 审计 → 转 HDF5。
若 zip 不在默认位置，用 `MCF_QPI_DATASET_ZIP=/path/to/MCF-QPI_数据集.zip` 指定。

---

## 6. 冒烟测试（验证 GPU 环境 + 代码链路）

```bash
bash scripts/run_smoke_test.sh
```
在 GPU 上会更快，且顺带验证 CUDA 前向/反向。出现 `17 passed` 与「Smoke test 全部完成」即通过。

---

## 7. 正式训练

以下命令用 0 号卡（`MCFQPI_DEVICE=cuda:0`）；想换卡改末尾数字即可，详见第 8 节。

```bash
# 主流程（一个随机种子）
MCFQPI_DEVICE=cuda:0 SEED=42 bash scripts/run_research_pipeline.sh

# 四项核心消融
MCFQPI_DEVICE=cuda:1 SEED=42 bash scripts/run_ablations.sh

# 扩散基线
MCFQPI_DEVICE=cuda:1 SEED=42 bash scripts/run_diffusion_baseline.sh

# 论文正式三随机种子（可在三张卡上并行：cuda:0/1，或分轮次串行跑 0 号卡）
MCFQPI_DEVICE=cuda:1 bash scripts/run_three_seeds.sh
```

产物在 `outputs/research/`（权重、逐样本 CSV、bootstrap CI、risk-coverage 曲线等）。

---

## 8. 设备与选卡说明（本次已改）

### 8.1 yaml 的 device 字段（持久默认）

| 配置 | device 字段 | 行为 |
|---|---|---|
| 正式训练 + 消融（`configs/research/`、`configs/ablations/`，10 个） | `cuda` | **无 GPU 直接报错退出**，不静默跑 CPU |
| 冒烟测试（`configs/smoke/`，5 个） | `auto` | 有 GPU 用 GPU，无则 CPU |

### 8.2 选卡：`MCFQPI_DEVICE` 环境变量（推荐，不动 yaml）

`src/mcfqpi/utils.py` 的 `select_device()` 现支持指定卡号，优先级从高到低：

1. **环境变量 `MCFQPI_DEVICE`**（最高，**推荐用法**）—— 不动 yaml 选卡；
2. 显式 `name`（来自 yaml 的 `device:`，或命令行 `--set device=...`）；
3. `"auto"`：有 GPU 用 GPU，否则 CPU。

取值：`cpu` / `cuda` / `cuda:0` / `cuda:1` / `cuda:N`。

**服务器实操（2× 3090 Ti）：**

```bash
# 用 0 号卡（你当前的选择）
MCFQPI_DEVICE=cuda:0 bash scripts/run_smoke_test.sh
MCFQPI_DEVICE=cuda:0 SEED=42 bash scripts/run_research_pipeline.sh

# 用 1 号卡（想让 0 号卡歇着、或同时跑第二个实验时）
MCFQPI_DEVICE=cuda:1 SEED=42 bash scripts/run_ablations.sh

# 两卡同时跑两个实验：开两个终端，分别 cuda:0 / cuda:1
```

### 8.3 其他选卡方式（备选）

| 方式 | 命令 | 适用 |
|---|---|---|
| 环境变量（推荐） | `MCFQPI_DEVICE=cuda:1 bash scripts/...` | 不改 yaml，最灵活 |
| 命令行覆盖 | `bash scripts/run_research_pipeline.sh --set device=cuda:1`（写入 resolved_config） | 只覆盖单次、但要留痕 |
| 操作系统级 | `CUDA_VISIBLE_DEVICES=1 bash scripts/...` | 连 cuDNN/NCCL 都只看该卡，最彻底；注意此时物理 1 号变成进程内 `cuda:0` |
| 改 yaml | 把 `device: cuda` 改成 `device: cuda:1` | 不推荐，会污染配置 |

### 8.4 索引越界保护

如果指定了不存在的索引（如 `cuda:5` 但只有 2 张卡），**立刻报错**，不会跑到一半才崩：

```
RuntimeError: 指定的 GPU 索引 5 超出范围：本机只检测到 2 张可用 GPU。
提示：若有多块卡但看不到全部，检查 CUDA_VISIBLE_DEVICES 是否限制了可见范围。
```

- 临时 CPU 调试：`--set device=cpu` 或 `MCFQPI_DEVICE=cpu`。
- 报错「配置要求 CUDA，但当前 PyTorch 未检测到可用 GPU」= 第 4 节没通过（装成 CPU 版 torch 或驱动不匹配）。

---

## 9. 常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| `torch.cuda.is_available()` 为 False | 装的是 CPU 版 torch | 按第 3 节用 `--index-url .../cu126` 重装 |
| 同上 | 驱动太旧，不支持所选 cuXXX | 用 `nvidia-smi` 确认后换更低的 cu 版本 |
| 训练报「配置要求 CUDA」 | 配置是 `cuda` 但环境无 GPU | 回到第 3/4 节，装好 CUDA torch |
| 报「指定的 GPU 索引 N 超出范围」 | `MCFQPI_DEVICE=cuda:N` 但 N≥卡数 | 改用存在的索引（0 或 1）；或检查 `CUDA_VISIBLE_DEVICES` 是否限制了可见范围 |
| OOM（CUDA out of memory） | batch size 太大 | 调小 `configs/research/*.yaml` 的 `loader.batch_size`；3090 Ti 24 GB 一般 `batch_size: 32` 够用 |
| 多卡只想用某张 | 默认 cuda:0 | `MCFQPI_DEVICE=cuda:1`（推荐，见 8.2）；或 `--set device=cuda:1`；或 `CUDA_VISIBLE_DEVICES=1` |
| 选了卡但没用上 | 环境变量没传进子进程 | 确认命令行是 `MCFQPI_DEVICE=cuda:0 bash ...`（等号紧贴、空格分隔），不是 `export` 后丢上下文 |

---

## 附：最少可跑通的一次性验证命令（装好环境后）

```bash
conda activate mcf-qpi
python -c "import torch; assert torch.cuda.is_available(), 'GPU 不可用'; print(torch.cuda.get_device_name(0))"
MCFQPI_DEVICE=cuda:0 bash scripts/run_smoke_test.sh
MCFQPI_DEVICE=cuda:0 SEED=42 bash scripts/run_research_pipeline.sh
```

> 跑起来后另开终端 `nvidia-smi` 应看到 GPU 0 的显存占用上升、GPU 1 保持空闲，即确认选卡生效。
